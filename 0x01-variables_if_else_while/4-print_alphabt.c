#include <stdio.h>
/**
 * main - main function
 * Return: always return 0
 */
int main(void)
{
	int n = 97;
	int m = 102;
	int q = 114;

	while (n < 101)
	{
		putchar(n);
		n++;
	}
	while (m < 113)
	{
		putchar(m);
		m++;
	}
	while (q < 123)
	{
		putchar(q);
		q++;
	}
	putchar('\n');
	return (0);
}
